{
  "mcpServers": {
    "code-search": {
      "command": "${CLAUDE_PLUGIN_ROOT}/.venv/bin/code-search-mcp",
      "args": [],
      "env": {
        "VOYAGE_API_KEY": "${VOYAGE_API_KEY}",
        "EMBEDDING_PROVIDER": "${EMBEDDING_PROVIDER}"
      },
      "type": "stdio"
    },
    "code-graph": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/codebase-memory-mcp",
      "args": [],
      "type": "stdio"
    }
  }
}
