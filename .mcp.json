{
  "mcpServers": {
    "code-search": {
      "command": "pythonw",
      "args": [
        "${CODE_SEARCH_PATH}/mcp_server/server.py"
      ],
      "env": {
        "VOYAGE_API_KEY": "${VOYAGE_API_KEY}",
        "EMBEDDING_PROVIDER": "${EMBEDDING_PROVIDER}"
      },
      "type": "stdio"
    },
    "code-graph": {
      "command": "${CODE_GRAPH_PATH}/codebase-memory-mcp",
      "args": [],
      "type": "stdio"
    }
  }
}
