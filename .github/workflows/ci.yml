# Managed by redacted-org/.github — do not edit directly
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  baseline:
    uses: redacted-org/.github/.github/workflows/baseline-ci.yml@main
