# Runtime state created by install.sh / install.ps1 inside the plugin directory.
# bin/ holds the committed launchers (bash and .cmd); installed components live in .runtime/.
.venv/
.venv.rollback.*/
.runtime/
.install-staging.*/

# Python
__pycache__/
*.py[cod]

# Generated comparison evidence (publish reviewed bundles separately).
bench/compare/runs/

# OS cruft
.DS_Store
